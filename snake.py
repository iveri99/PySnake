import pygame
import random

pygame.init() # Initialise pygame

# Constants
HEIGHT = 600
WIDTH = 800 
CELL_SIZE = 20 
WHITE = (255, 255, 255)
BLACK = (0, 0, 0)
GREEN = (0, 255, 0)
RED = (255, 0, 0)

screen = pygame.display.set_mode((800,800)) # Set up screen/display

